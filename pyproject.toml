[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bnlnpps-swf-common-lib"
version = "0.0.1"
description = "A common library for SWF components."
readme = "README.md"
requires-python = ">=3.9"

[project.urls]
"Repository" = "https://github.com/bnlnpps/swf-common-lib"
